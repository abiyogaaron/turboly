%nav{:class => "navbar navbar-dark bg-dark"}
	.container.d-flex.justify-content-between

		%a{:class => "navbar-brand", :href => "../"}
			%img{:src => asset_path('logo.png'), :width => "115", :height => "32", :class => "d-inline-bl"}
		- if !user_signed_in? 
			= link_to('Sign in', new_user_session_path, class: "btn btn-outline-light btn-signin", role:"button")
		- else
			= link_to('Sign out', destroy_user_session_path, method: :delete, class: "btn btn-outline-light btn-signout", role:"button")
